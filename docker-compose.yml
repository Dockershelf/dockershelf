services:
  app:
    image: dockershelf/dockershelf:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/home/dockershelf/app
      - ~/Library/Caches/pip:/home/dockershelf/.cache/pip
